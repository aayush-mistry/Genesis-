export const PerceptionConfig = {
  /**
   * The default maximum distance (in spatial units/meters) that a citizen
   * can perceive resources, buildings, and entities around them.
   */
  DEFAULT_PERCEPTION_RADIUS: 2000,
};
