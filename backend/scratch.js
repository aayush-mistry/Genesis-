const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { console.log('Cities:', await prisma.city.count()); console.log('Districts:', await prisma.district.count()); console.log('Buildings:', await prisma.building.count()); console.log('Workplaces:', await prisma.workplace.count()); console.log('Households:', await prisma.household.count()); console.log('Citizens:', await prisma.citizen.count()); console.log('Building Types:', await prisma.building.groupBy({ by: ['type'], _count: true })); console.log('Workplace Types:', await prisma.workplace.groupBy({ by: ['type'], _count: true })); } run().finally(() => prisma.$disconnect());
